server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jgraneros/config-server.git
          default-label: main
          clone-on-start: true


